name: odc_market_place
description: "Sandhai Rate - Daily Market Prices for Oddanchatram & Dindigul"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4

  # State Management

  # UI & Fonts
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1

  # Utils
  intl: ^0.20.2
  shared_preferences: ^2.3.2
  cupertino_icons: ^1.0.8

  # Ads
  google_mobile_ads: ^5.1.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  get_it: ^9.2.1
  dio: ^5.9.2
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

